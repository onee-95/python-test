steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'catnsoup/appsflyer/aos-all:latest', '.']
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'catnsoup/appsflyer/aos-all:latest']

options:
  logging: CLOUD_LOGGING_ONLY
